apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quote-chart.fullname" . }}-test-connection"
  labels:
    {{- include "quote-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quote-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
